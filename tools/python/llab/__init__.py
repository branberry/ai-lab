"""llab — ai-lab shared Python harness.

Modules:
- llab.client   — unified generate() routing to Ollama or provider APIs.
- llab.eval      — minimal eval runner.
- llab.models    — local Ollama model helpers.
- llab.probes    — interpretability helpers (skeleton).
- llab.cli       — command-line entry point.
"""

__version__ = "0.0.1"
